# Client

Use the repo root README for setup and app launch.

Canonical workflow:

```powershell
cd C:\sb\playground\RetroBoard
npm run setup
npm start
```

Open `http://localhost:8081`.

Demo mode without the API server:

```powershell
cd C:\sb\playground\RetroBoard
npm run setup:demo
npm run demo
```
