= RetroBoard

The canonical setup and run instructions now live in `README.md`.

Use the repo root and run:

----
npm run setup
npm start
----

Then open `http://localhost:8081`.

For a no-server demo:

----
npm run setup:demo
npm run demo
----

For demo mode with live Jira pulls through a local bridge:

----
npm run demo:jira
----
